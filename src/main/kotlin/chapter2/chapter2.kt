package chapter2

/**
 * 함수 타입
 *
 * 함수 타입으로 정의된 함수는 invoke 메서드로 호출 가능
 * 함수마다 매개변수와 결과 타입이 다르므로 함수 타입의 종류 또한 다양하다.
 */

/**
 * boolean 반환 함수 타입: predicate
 *
 * 값 하나를 다른 하나로 변환하는 함수: transform
 *
 * Unit 반환 함수: operation
 */

/**
 * 함수 타입 호출
 *
 * 제공 메서드: invoke
 *
 * invoke는 연산자기 때문에 객체를 호출 할 수 있음 > 암묵 호출
 *
 *  * 코틀린에서의 연산자는 메서드의 다른 표현. operator 키워드를 사용하면 메서드를 연산자로도 사용 가능하다는 의미
 */

/**
 * 타입 별명
 *
 * 지정: typealias
 */


/**
 * 함수 타입의 내부: 제네릭 타입 매개 변수를 받는 인터페이스
 */

